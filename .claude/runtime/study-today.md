<!-- generated by study-brief.py | date=2026-08-05 -->
# 오늘의 학습 — 2026-08-05 (수)

> **검토 요약 (15:44)**: **W3 D2 완료 ✅** — 20일 공백을 "막힌 게이트(gpu-access-decision.md 공란)"로 스스로 진단하고 같은 세션에 해소한 게 최고 산출물. 주의 2건: ① `verify-gpu-container.sh` Step 3이 pull 출력을 CID로 오파싱해 **성공을 실패로 기록**한다(이미지 미보유 시 = VM 첫 실행 시 항상) ② Step 5의 `find … || echo`는 매치 0건에도 exit 0이라 **"(없음)"이 절대 안 찍힘** — fail-silent 재발(감시 ⑦). W4 게이트는 **스팟 회수 대응 + 후보 비교표 미기재로 체크 보류** — 15분이면 W4 D1~D4를 한 번에 마감할 수 있다.

## 오늘 할 것 (평일 · 20~40분)
**W3** · D3: CPU Mac에서 가능한 실측 — 컨테이너 안 드라이버 부재 확인

### 학습 가이드
- 📖 자료: `docker run --rm mnist-train:multistage sh -c "ls /dev | grep -i nvidia; ldconfig -p | grep -i cuda"` → 이어서 `--gpus all` 시도 시 에러 관찰
- ✅ 완료: 출력과 에러를 log.md "발생한 문제/확인" 칸에 기록 — **에러가 나는 게 정상인 실험**

> 피곤하면 10분 대안: 어제 셀/스크립트 1개 다시 실행 + 변수 1개 바꿔 결과 비교 후 커밋. 연속성만 유지.

## 어제 한 것 검토
`/study-coach review` 실행 → 어제 산출물(`~/Desktop/Project/ai-infra-lab`의 커밋·코드·docs/log.md)을 점검하고 진도를 체크합니다.

## 세션 마감 형식
```
오늘 한 것:
발생한 문제:
해결하거나 확인한 내용:
다음에 할 것:
```
