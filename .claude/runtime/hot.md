# hot — session boot context (L1)

> AI runtime cache, not for humans. Only the block between INJECT markers is injected by the `session-context` hook (stable → prompt-cache friendly). Rolling detail below the marker is drill-down only.
> Per [[context-layering]]: keep the injected block stable; append session notes under "Recent sessions" (outside the marker) and trim to ~500 words rolling. Language: English (AI hot path). Shared knowledge in `wiki/` stays Korean.

<!-- INJECT:START — session-context 훅이 이 블록만 주입. 안정 유지(캐시 친화). -->
## Navigation
L1 hot.md → L2 MOC (`80 Tooling/80 Tooling.md`) → L3 notes. Read the cheapest layer that answers first.

## Vault state
- Content (**§11 numbered-folder layout, adopted 2026-08-22**): topic dirs are §11 folders, each still carrying a `<dir>/<dir>.md` MOC — **MOC detection is now path-based (filename == parent dir name)**, since §12 dropped the `moc` type value. **`80 Tooling/`** = Claude Code official-docs KB (code.claude.com/docs, 145 pages) — 29 Korean notes (27 from docs + 28 = Anthropic-blog large-scale-migration how-to + 29 = self-hosting environments) plus the absorbed vault self-diagnosis note (`00 LLM Wiki 아키텍처와 OKF 자기진단`, ex-`Meta/`), and `80 Tooling.md` MOC (title stays "Claude Code 사용법"; it gained a "vault 자기성찰" section carrying the retired Meta hub's content). Coverage 145/145, broken links 0. **`20 Architecture/`** = infra CS·Linux·kernel fundamentals, 10 notes (ex-`Infra/`) + MOC — foundation layer under AI-Infra. **`30 AI Infrastructure/`** = backend→AI-infra learning, 8 notes (ex-`AI-Infra/`) + MOC, cross-linked to 20 Architecture at MOC level. Empty §11 slots awaiting content (`.gitkeep` only): `00 Inbox` · `10 Evergreen` · `40 Backend` · `50 Cloud and Kubernetes` · `60 Security` · `70 Career` · `90 Archive`. `Templates/permanent-note.md` = §13 10-section template; `Templates/`·`Attachments/`·`00 Inbox/` are kb-lint-excluded (not notes). Old wiki/·sources/·index.md·log.md were **intentionally deleted 2026-06-07** (recoverable via git history — do NOT suggest restore). Navigation: L1 hot → **L1.5 topic router (this Content line)** → L2 `<dir>/<dir>.md` → L3 notes. ⚠ **§11 numbers do NOT match Notion's** (Notion 40=Archive / 90=Weekly Review vs here 40=Backend / 90=Archive) — never carry a bare "put it in 40" instruction across the two systems. ⚠ 58% of notes (the 30 in `80 Tooling/`) have no natural §11 home; slot 80 was chosen to keep them one block rather than shredding the single hub across six folders. Both caveats were reported before adoption and the decision was reaffirmed — they are open debts, not settled design.
- Framework (`.claude/`, rebuilt 2026-06-07): commands `/kb-query` `/kb-sync` `/kb-lint` `/kb-ingest` `/kb-save` `/claude-radar` `/study-coach` `/skill-audit` (business-track commands/agents rolled back 2026-06-09; zombie TestFounderOrg removed 2026-07-07) · agents: kb-guide(read-only Q&A, sonnet) · kb-updater(heavy KB writes, sonnet; hot.md stays main-session duty) · soobeen-voice(1st-person TIL/retro drafts, sonnet, evidence-only from log.md+review-log; style canon = user's real blog posts via RSS — concept-explainer structure, no internal-curriculum jargon (2026-07-07 user feedback); draft-only — no Write/Edit, interactive-only; images = numbered placeholders `[사진 N]` + end build-section image-plan classifying each `web`/`shot`; main session searches URLs for `web` items then blog-collect.py downloads them into `blog/<slug>/N. name.png` (+SOURCES.md provenance), `shot`=user uploads (SVG auto-gen+blog-assets.py dropped, replaced by web-collection 2026-07-08)) · skills kb-assistant(intent routing) + soobeen-check(personal session-close check vs watch-list ①~⑦, ai-infra-lab read-only, interactive-only) + blog-publish(semi-auto Tistory post via **aside-browser `aside repl`** (migrated off the disabled claude-in-chrome 2026-08-22; `page.evaluate(fn, arg)` + repl `fs` removed the old local-http-server/CORS workaround): markdown-mode title/body + `[사진 N]` position markers + **temp-save only**; image attach + final publish stay user-manual per automation-safety; interactive-only, not-in-cron; 2026-07-08; pitfall doc'd 2026-07-10: page has TWO CodeMirrors — inject into `.cm-s-tistory-markdown`, not the hidden html one) · rules vault-rules+automation-safety+soobeen-profile(user persona: comms contract + watch-list ①~⑦ + coaching principles w/ verification status, evidence-audited 2026-07-07, ≤150 lines; ⑤ narrowed 2026-07-09 — user-confirmed deliberate transcription of AI-set next-plan ≠ delegation) (all auto-loaded) · hooks (4 scripts / 4 events): session-context(SessionStart, injects this block + radar-queue pending + study-today brief) · kb-lint-check→secret-scan(PostToolUse) · auto-commit(Stop+SessionEnd, fetch-guarded push; vault marker = CLAUDE.md OR `.claude/` — fixed reset-era no-op where missing root CLAUDE.md silently skipped all commits) · scripts kb-lint.py(machine lint; `type` closed-enum + MOC-backlink + age/governance metrics; `--online` diffs sources vs llms.txt; `--links` external-URL liveness) · kb-source-hashes.py(source content-hash drift → review queue, no auto-edit) · scrub-secrets.py · radar-collect.py(0-LLM 8-source radar) · study-brief.py(0-LLM daily study-coach brief: study-state.md item + 학습 가이드(개념/자료/완료/막히면) → study-today.md, date-idempotent; `--brief-only` writes brief without touching last_brief_date = cron LLM-failure fallback) · blog-collect.py(0-LLM; collects soobeen-voice draft's `web` images → body `[사진 N]`↔`IMG:` plan 1:1 check(exit 4) + download to `blog/<slug>/N. name.png`(content-type image/* + 10MB cap + SSRF guard: http/https·public-host only) + SOURCES.md provenance + build-section strip; `shot`/no-URL=pending; URL search=main-session, script does download/validate/organize only; main-session-run, not agent/cron; `--check`/`--allow-local-hosts`(test); 2026-07-08) · stray-guard.sh(cron STRAY revert; `runtime` mode=radar+study / `kb` mode=kb-sync, shared by all cron wrappers) · tests `bash .claude/tests/run-tests.sh` (**183 cases, all green 2026-08-22**; +blog-publish skill static contracts(4: name/temp-save-only/images-not-auto-attached/not-in-cron); +blog-collect contracts(7: check/placeholder-manifest-mismatch-exit4/web-download+SOURCES/build-strip/SSRF-block/non-image-reject via local http server) + blog/ exclusion(kb-lint batch+hook, inherits parent suite like Projects/); soobeen-voice image contract(1) asserts `[사진 N]`+web/shot plan+blog-collect.py + absence of blog-assets/SVG refs (SVG/rasterize pipeline retired, replaced by web-collection 2026-07-08); +soobeen-persona static contracts(7: draft-only/read-only/≤150-lines/not-in-cron), −TestFounderOrg(5 zombie tests for rolled-back mechanisms), Projects/-exclusion restored in kb-lint.py+kb-lint-check.py — rollback had dropped it while hot.md+tests kept the contract). **Frontmatter = §12 11 fields: id/title/type/status/created/updated/area/tags/source_urls/notion_url/confidentiality** (canonical `.claude/kb-required-fields.txt`; type enum `.claude/kb-allowed-types.txt` = evergreen|concept|architecture|comparison|playbook|career — `moc` value is gone, so MOC detection moved to *filename == parent-dir name*, the same basis the backlink check already used). block-style YAML `source_urls:` lists parse (was silent-dropped — P0 fix). kb-lint excludes root-level `.md` + `.trash` + `Projects/` (business ops workspace) + `blog/` (blog drafts + collected images, git-tracked but not KB content).
- Cron: launchd `com.<user>.kb-sync` runs `.claude/kb-sync-cron.sh` Mon·Thu 09:07 local + RunAtLoad — headless `claude -p "/kb-sync"` (sonnet, acceptEdits, Bash curl/python3 only). **Anacron catch-up**: stamp `.claude/runtime/kb-sync-last-run` (epoch, updated only on success); every invocation no-ops unless latest slot > stamp, so login-after-poweroff backfills a missed slot (sleep-missed slots fire natively on wake). Slot times in wrapper's python block MUST match plist's StartCalendarInterval. Portable paths; reinstall on a new machine via `bash .claude/install-kb-sync-cron.sh`. Log: `.claude/runtime/kb-sync-cron.log`. No contract test yet for the wrapper — add one on next test-suite touch.
- Radar cron: launchd `com.<user>.claude-radar` runs `.claude/claude-radar-cron.sh` **daily 09:30** + RunAtLoad — headless `/claude-radar collect` (sonnet, Bash python3 only). `radar-collect.py` scrapes 2 topics: **Claude Code** (HN Algolia · GitHub releases.atom + topic search · awesome-claude-code/subagents commits.atom · GeekNews rss/news · Anthropic release-notes · dev.to · npm) + **AI-Infra learning** (vLLM/KServe/Karpenter releases.atom · HN AI-infra keywords, source-tagged `AI-infra:` → routed to `30 AI Infrastructure/` KB) → dedup via `runtime/radar-seen.json` (first run = baseline; 120d/3000 prune; per-source cap 8) → session appends recs to `runtime/radar-queue.md`. **Collect writes ONLY queue+ledger — never .claude/·KB.** Generation = `/claude-radar review` (interactive, consent-gated): skill-creator for skills, `.claude/{agents,commands,rules}/*.md` else, `/kb-ingest` for KB. Pending surfaced at SessionStart by session-context. Anacron stamp `runtime/radar-last-run` (slot 09:30 MUST match plist). Install: `bash .claude/install-claude-radar-cron.sh`. Log: `runtime/radar-cron.log`. Canonical rule in vault-rules.md.
- Study-coach cron: launchd `com.<user>.study-coach` runs `.claude/study-coach-cron.sh` **daily 09:30** + RunAtLoad — `git pull --ff-only` then headless `/study-coach review` (sonnet; allowlist Bash(python3 study-brief.py)+Bash(git)+Read/Write/Edit). LLM reviews yesterday's output in the **separate `ai-infra-lab` repo (READ-ONLY)**, grades it, checks off completed items in `runtime/study-state.md`, then runs `study-brief.py` to write today's `runtime/study-today.md` + macOS notification. **Usage-limit resilient (added 2026-07-02): if the LLM review dies (session limit/network), the wrapper still runs `study-brief.py --brief-only` so today's brief always exists; `--brief-only` leaves `last_brief_date`+stamp untouched so LLM grading retries after the limit resets.** **Writes ONLY `.claude/runtime/` — ai-infra-lab read-only, .claude mechanisms/KB reverted by stray-guard(runtime mode).** Multi-machine: `study-state.md`(tracked, shared) holds progress; `last_brief_date` = 2nd idempotency key so two Macs don't double-review; anacron stamp `runtime/study-last-run`(per-machine, gitignored). repo path via state meta `repo_path` + `runtime/study-local.conf`(gitignored, per-machine override). Install per studying Mac: `bash .claude/install-study-coach-cron.sh` (skip on a Mac where unattended review isn't wanted → run `/study-coach review` manually). Log: `runtime/study-cron.log`. Canonical rule in vault-rules.md. Curriculum state: W1~W2 done 2026-07-12 (Block 0 + Block 1 W1 — multistage Dockerfile shipped); W3~W4 (Block 1 rest: Container Toolkit/NGC + gpu-access-decision gate, weekend smoke test user-approved ~$5) detailed via `/study-coach plan` 2026-07-12.
- Update duty: **3 steps**, canonical in `.claude/rules/vault-rules.md` (bump `updated` → reflect in MOC → one English hot.md line). Old 4-step (index.md/log.md) retired.
- Change-sync duty: root `CLAUDE.md` (new 2026-06-08, Korean, **wins on conflict**) maps what-changed→what-to-update — framework(`.claude/`)→README+hot+test; KB→vault-rules 3-step; cron→README+hot. Any add/edit must sync its doc in the same change.
- `.codex/` adapter **removed 2026-06-19** (was stale + never registered in settings.json). No multi-runtime adapter now; add AGENTS.md + `@AGENTS.md` import only if other-agent interop becomes a real need (see EXTENSIBILITY.md).
- Caveat: `80 Tooling/80 Tooling.md` ≈ CLAUDE.md on case-insensitive APFS → auto-injected as context when working under `80 Tooling/`. Keep the MOC lean.

## In progress
- **AI-infra capstone (2026-06-09): `vllm-spot-bench`** — 사업 발굴 트랙 전부 롤백 후(geo·agent-ops·직원조직7·커맨드3 삭제, kb-lint/test/README/hot e1be8c2 복원) "AI 인프라 엔지니어 성장" 방향으로 전환. 캡스톤 레포 scaffold 완료: `~/Desktop/Project/vllm-spot-bench`(vault 밖, 별도 git) — serve.sh·bench(run_bench.sh·k6 load.js)·metrics/cost_per_token.py·README(벤치표+백엔드서사). 가이드=`30 AI Infrastructure/10 핸즈온`. **다음: ①GPU 없이 0단계 로컬 워밍업(Qwen-1.5B) or ②GPU 확보(Vast.ai RTX4090 최저가 / AWS g5 스팟=AWS트랙) → serve→bench(동시성 1·5·10·20 스윕)→README 표·$/1M tokens 채우기→GitHub 공개.** 완료기준=30 AI Infrastructure/10 체크리스트. **학습 동반**: `30 AI Infrastructure/11`(캡스톤=시스템 CS 학습 가이드 — do→CS→실무→면접→이해체크; M1 서빙/KV캐시=OS페이징 깊이, M2~5는 진행하며 추가) — 사용자 요구 "단순 복붙 아닌 CS 이해하며 성장" 반영.
- KB maintenance loop: run `/kb-sync` weekly-ish (official docs ship whats-new pages weekly); `/kb-lint --online` verifies coverage. Slug-level diff only — content changes inside existing pages need `--deep`.
- claude-radar (2026-06-08): daily Claude-ecosystem radar — **active** (launchd installed, daily 09:30; seen ledger seeded with 60 items). First review done: added `/skill-audit` command + `automation-safety-rules.md`, ingested 5 items into KB (04 설정·08 서브에이전트·19 비용·20 베스트프랙티스·26 변경이력). Ongoing: queue fills daily, review via `/claude-radar review`.
<!-- INJECT:END -->

## Recent sessions (newest first)
- **2026-08-22 (3)** — **§11/§12 doc sync finished.** The migration's prose had been left stale in ~14 files; closed it. `vault-rules.md` Note format rewritten field-by-field (all 11, each with its actual current value and its debt: `notion_url` is `""` everywhere, `tags` is one machine-derived tag pending human curation, `confidentiality` is a **label with no mechanical gate** on a vault that auto-pushes to a public remote). Two canonical statements were **falsified by the migration and are now corrected rather than left to rot**: (a) the macOS APFS caveat — the old `Claude/Claude.md`↔`CLAUDE.md` collision is **gone** as a side effect of the rename, so no topic MOC shadows the memory file anymore; (b) the non-goal "`type` is the only frontmatter axis we add", now carrying an explicit `> [!warning] 결정 변경` block recording that §12 added 7 axes by user decision, that the concerns were reported and reaffirmed, and that they are open debts rather than settled design (typed *internal* relation fields stay rejected). Commands/agents de-duplicated toward the canon: `kb-ingest`/`kb-save`/`kb-sync`/`kb-updater` no longer re-spec the field list (they point at `kb-required-fields.txt`), `sources`→`source_urls` and `type: reference`→`playbook` throughout, `허브: [[Claude]]`→`[[80 Tooling]]`. README tree + hot Content line rebuilt for the numbered layout. **Silent-breakage check passed**: the rename's two exit-0 risk paths were exercised live — `kb-lint --online` read 188 KB slugs and surfaced real drift (+managed-settings/settings-example/settings-reference, −agent-sdk/slash-commands/ultraplan → next `/kb-sync`), and `kb-source-hashes.collect_sources` returned 253 items. Neither returned 0, which is what a broken field name would have produced while still exiting 0. Tests 183 green, kb-lint 51 notes clean.
- **2026-08-22 (2)** — **Skill set pruned + browser layer unified on Aside.** Deleted 2 user-scope skills (`~/.claude/skills/algorithmic-art`, `doc-coauthoring`) — they were stale **Korean forks (2026-03-21)** of upstream plugin skills that are still live as `example-skills:*` (2026-04-09), so the duplicate names made skill selection non-deterministic every session at zero capability gain. Archived to `~/.claude/removed-skills-20260822.tar.gz` first (user scope is outside git — deletion is not recoverable there). Kept the rest after checking each has a real consumer: `pydantic-yaml-config`→smart-agent, `merge-worktree`→webin (only repo with >1 worktree), `orca-*`/`computer-use`→orca, `aside-browser` (canonical browser layer), `manage-skills`+`verify-implementation` (registered in global CLAUDE.md). **`blog-publish` re-based onto aside-browser** — all hard-won Tistory knowledge preserved (two CodeMirrors → inject into `.cm-s-tistory-markdown`; title needs real input events or it saves blank; `type`-style synthesis breaks Korean jamo → use `keyboard.insertText`; native file dialog means images stay manual). The Aside repl's `fs` + `page.evaluate(fn, arg)` let the body be passed directly, deleting the old local-http-server + CORS `fetch()` workaround entirely. **`/kb-ingest` gained an Aside fallback** for login/paywall-blocked URLs (402/403) — the 2026-07-27 entry below is the recorded failure that motivated it. `kb-assistant` updated for the §11/§12 migration (`Claude/`→`80 Tooling/`, 4 fields→11). New contract test `test_browser_layer_is_aside_not_chrome` blocks the regression mechanically: it permits a "do not revive" note but fails if claude-in-chrome appears in the description or in any line lacking a negation. Tests 182→183.
- **2026-08-22** — **Second-brain inspect (Notion + Aside + vault, 12-agent workflow) + 26-day git divergence resolved.** Notion `Second Brain · 수빈` read via Aside's Notion MCP (read-only): skeleton only — `10 · Projects` 0 real projects, `90 · Weekly Review` 0 logs, 업무 수집함 7 rows (5 title-only, 1 blank), 개발자 학습 브리핑 19 rows (12 blank, 1 schema-filled). The hybrid-design premise inverts: Notion is the empty side, this vault is the running system. **Decisions: §11 folder restructure and §12 11-field frontmatter NOT adopted** — type enum intersection is 0, so `moc` disappears and MOC backlink enforcement (`kb-lint.py:232`) dies silently; `sources`→`source_urls` breaks `--online` slug diff + source-hashes while keeping exit 0. `confidentiality` deferred too: a label with no mechanical gate is what automation-safety forbids. **git**: merged `origin/main` (not rebase — ledger conflicts would repeat per commit). Sole conflict `radar-seen.json` resolved as key union, earliest first-seen wins (1448+1129 -> 1471, 0 key loss). Backup ref `backup/pre-merge-20260822`. **Leak paths closed**: `.smtcmp_json_db/` (Smart Composer chat DB) untracked + gitignored; `Tags/` gitignored (was neither tracked nor ignored -> `git add -A` exposure path); `study-brief.py` no longer bakes `$HOME` into the tracked `study-today.md` (+contract test -> 182 cases). Doc counts corrected across CLAUDE.md / vault-rules / README / hot (Claude 29, AI-Infra 8). **Standing user decisions (recorded, not re-litigated)**: the PAT stays in `.git/config`; `llm-wiki` stays PUBLIC. Cross-ref the 2026-06-01 entry below: that PAT placement is a **regression** of the keychain migration done then, and the token was already flagged as leaked into chat logs — rotation remains outstanding by the owner's choice. **Not fixed (outside vault scope)**: global hook `~/.claude/hooks/commit-session.sh:82` authors the 34 `wip:` commits via `git add -A`, no junk guard, repo resolved from cwd — a second commit path that bypasses this vault's guards. **Open**: hot.md is 3.3k words vs the ~500-word contract (trim needs owner approval — it deletes session records).
- **2026-07-27 (3)** — **kb-ingest: Cerebras KB architecture → 30 AI Infrastructure/04.** User gave an X/tweet URL (auth-walled: curl/WebFetch 402, claude-in-chrome extension down, Orca browser not logged into X). Recovered the public canonical source from the tweet preview (Orca browser eval on the public tweet page) → `cerebras.ai/blog/how-we-built-our-knowledge-base` (2026-07-15), rendered via Orca browser. New note `30 AI Infrastructure/04 RAG 지식베이스 검색 아키텍처 (Cerebras 사례)` (type: explanation): single pgvector table + per-source connectors, Slack hybrid retrieval (FTS+embedding+IDF+age-decay)+distillation+bursting, CocoIndex code embeddings, RRF(k=60)+LLM rerank, MCP LLM-free tools orchestrated by Claude Code. Scrub clean. Cross-linked to `[[10 MCP]]` + Meta `[[00 LLM Wiki 아키텍처와 OKF 자기진단]]` (this IS the deferred RAG-non-goal blueprint: FTS+embedding+RRF). AI-Infra MOC gets new "사례 연구" section, `updated` bumped. (kb-save then completed the reverse link: Meta `00` RAG-non-goal bullet → `[[04 …]]` as the reference design, `updated` bumped — bidirectional now.)
- **2026-07-27 (2)** — **kb-sync: 2 new official slugs absorbed.** slug-diff vs llms.txt found `claude-security` + `desktop-ios-simulator` (0 gone). Added as sections to existing notes (no new note): `claude-security` → `Claude/18 보안과 샌드박스` §10 (multi-agent vuln-scan plugin `/claude-security`, scan→patch flow, defense-in-depth stack vs security-guidance/`/security-review`/Code Review); `desktop-ios-simulator` → `Claude/14 IDE와 데스크톱` (iOS Simulator pane, macOS Desktop beta, per-device consent + managed-setting kill-switches). Both `sources`/`updated` bumped, MOC summaries updated, kb-lint `--online` = 172↔172 parity (was 170). (Note: KB sources has a stray `to` slug — not a docs page; investigate next kb-sync.)
- **2026-07-27** — **claude-radar review → KB ingest.** Added `Claude/28 대규모 코드 마이그레이션` (how-to, synthesized by kb-updater from Anthropic blog `claude.com/blog/ai-code-migration` 2026-07-16 — judge-first prep → rulebook/dep-map/gap-inventory → rule stress-test → multi-agent translation loop (2 adversarial reviewers + 1 judge) → compile/run/behavior-diff; Bun Zig→Rust + Python→TS cases, model role-split, cost-benefit). Claude MOC 27→28, kb-lint clean. Queue: item ① [done], ② obsidian-llm-wiki [dismissed]. Note 28 is blog-sourced (not code.claude.com/docs) — MOC intro adjusted; revisit MOC intro wording if blog-sourced notes grow.
- **2026-07-07** — **Persona mechanisms ×3** (evidence-audited via 13-agent workflow vs study-state review-log + ai-infra-lab): soobeen-voice agent (1st-person TIL drafts, draft-only) · soobeen-check skill (session-close check vs watch-list ①~⑦) · soobeen-profile rule (35 lines). Profile corrections: "초과 달성"→burst-type completion; +⑥ commit hygiene (fence-polluted messages ×6, low-info titles ×4); +⑦ fail-silent coding ×3; blank-template coaching marked unverified-prescription. Baseline repairs: −TestFounderOrg (5 zombie tests), Projects/ exclusion restored (kb-lint.py + kb-lint-check.py), kb-assistant "3필드"→4, README counts fixed (79→135). Tests 135 OK. Schema: 30 AI Infrastructure/11 got missing `type: tutorial` (47/48→48/48, no updated-bump per migration rule).
- **2026-06-07 (2)** — **Framework rebuilt: wiki → KB.** User picked "remove old + new only". Removed (git-recoverable): 7 wiki commands, 3 agents, wiki-assistant skill, 3 dormant hooks (protect-sources·wiki-lint-check·capture-candidate), 4 weekly-lint scripts, wiki-page-rules, wiki-required-fields. New: 5 kb commands, kb-guide/kb-updater agents, kb-assistant skill, kb-lint.py(+kb-required-fields.txt), kb-lint-check hook, vault-rules.md rewritten (4-step duty → 3-step), EXTENSIBILITY.md updated, tests rewritten (43 pass). settings.json rewired with explicit user approval (PreToolUse removed; PostToolUse kb-lint-check→secret-scan). Workflow: 5 authors ∥ → tests → 2-lens verify (8 blocking issues found+fixed: MOC sources-exemption bug in hook, .agents/ link-resolution drift, update-duty re-spec drift in 3 commands+skill). `.codex/` left stale intentionally. launchd weekly-lint was never installed — nothing to remove.
- **2026-06-07** — **Claude Code docs KB built in `80 Tooling/`** (user request, vault intentionally cleared beforehand — deletion confirmed intentional, not restored). Workflow fan-out: 26 cluster agents fetched all 145 raw `.md` pages from code.claude.com/docs (curl, not WebFetch summarization) → 26 Korean usage-reference notes + `Claude.md` MOC (learning paths + full map). Verified independently: sources coverage 145/145, broken wikilinks 0, CLI flags 81/81 (2 long-forms patched post-hoc), hook events all present. Caveat: llms.txt index = 145 pages (WebFetch's "181" was small-model overcount). `80 Tooling/80 Tooling.md` doubles as CLAUDE.md on APFS (case-insensitive) — auto-injected when working in Claude/.
- **2026-06-02** — **Ingest: SRE for AI (Google, GeekNews).** Source = sre.google "AI for Reliable Operations" (GeekNews relay). 2 new pages: [[progressive-autonomy]] (concept·ai-engineering·active) = vendor-independent autonomy-governance frame — L0–L4 across 5 axes (monitor/investigate/approve/act/self-direct), progression gates, Safety Trifecta (transparency=CoT log / real-time risk eval / progressive authz), architectural guardrails (least-privilege·circuit-breaker·dry-run·zero-trust·Red Button), dynamic L3→L2 downgrade; maps to harness L·V axes. [[sre-for-ai-operations]] (synthesis·ai-engineering·active) = Google worked instance — 7 systems (AI Operator L3, Actus 3-stage control plane, IRM Analyzer, Detectr, InvD, AI Alert read-only, Antigravity CLI), golden-data 3-tier (Bronze/Silver/Gold) + nightly LLM-as-a-Judge + deterministic scoring, Intervening PR→feature-flag+Fix-Forward, MTTM 44%/10%. Updated agent-harness-taxonomy (Safety Trifecta→V, progressive-authz→L), production-llm-backend (SRE reliability para), guardrails (Trifecta as action-level guardrail), ai-engineering-moc (new "SRE & autonomy" section). Cross-links to [[llm-oriented-engineering]] (human owns policy) + [[self-improving-system]] (judge validity). MTTM figures flagged `> [!warning]` (baseline unspecified). scrub clean (0 masked), lint 0 (42 pages).
- **2026-06-02** — **Ingest: GEO (geo-seo-claude, GeekNews).** New [[generative-engine-optimization]] (concept·ai-engineering·active): AI search citation optimization — signal shift backlinks→brand-mentions (3x), 134-167-word citable passages, 14+ AI crawlers, only 11% domains cited by both ChatGPT+Google AIO. geo-seo-claude (MIT ★7.8k) = worked instance (5 parallel subagents → cross-linked [[claude-code-subagents]]; skill .venv isolation → [[claude-code-best-practices]]). RAG mirror-image cross-link to [[rag-patterns]] (citability = supplier-side retrieval). llms.txt utility flagged as contradiction (tool generates vs Google guide says unneeded). scrub clean, lint 0.
- **2026-06-02** — **.codex adapter doc reconciliation** (drift fix). `.codex/` Codex CLI port was committed (10 files, 46 tests) but 0 mention in canonical docs. Added port section to extension-architecture + CLAUDE.md + EXTENSIBILITY sync checklist + hot.md (hooks 5→6, secret-scan was missing). Committed+pushed 2cfcfec.
- **2026-06-01** — **Framework review (6-dim workflow, 32 agents) + fixes.** Verdict: exemplary single-runtime LLM Wiki; 2 systemic gaps. **Cluster 1 (multi-runtime drift) fixed:** `.codex/hooks.json` was missing SessionEnd → Codex sessions never ran git push/divergence-guard (HIGH); resolved dual source-of-truth (AGENTS.md now = mirror of canonical CLAUDE.md; README canonical list fixed); fixed dead pointer `Codex-extension-architecture.md`→`claude-extension-architecture.md`; EXTENSIBILITY.md gained an adapter-sync section (+ stack-order contract, model-tier asymmetry note); count drift fixed (hooks 5→6, dropped hardcoded test counts). **Parity tests added** (`.claude`↔`.codex` hook-set + event-set equality — would have caught the SessionEnd drift). **graph-1:** revived dead MOCs via `도메인 지도` backlinks (llm-wiki-moc inbound 0→4, pkm-moc 0→1). 48/48 tests, lint 0. **Deferred:** adapter generator (replace hand-copy); cluster 2 = build→ingest tilt + query/save reuse loop (still ~6%).
- **2026-06-01** — **Ingest: Software After AI + LLM Oriented Engineering.** Added 2 source digests (Tunguz, Weinberger) + [[llm-oriented-engineering]]. Updated [[agent-harness]]/[[agent-harness-taxonomy]] with Tunguz 7-part product/ops checklist over 6-tuple; updated [[self-improving-system]] with tests/evals/clean-context judge as reward function and human gate for modeling/API contracts. Code logic change not needed: current hooks/tests/lint/context-layering/skill-adapter structure already aligns.
- **2026-06-01** — **First domain ingest: Claude Code ×4** (meta→tooling tilt). Dynamic Workflow: 4 topics parallel deep-research → adversarial verify (all reliable/high) → main baked sources(+scrub, clean) + 4 wiki pages (tooling·stable): [[claude-code-plugins]] (plugin.json·marketplace·path vars·vault-as-plugin path) · [[claude-code-hooks-reference]] (full event catalog, cadence×3 + 2026 plugin events, matcher, 5 handler types) · [[claude-code-subagents]] (15 frontmatter fields·memory·Agent SDK·v0.1.0 rename) · [[claude-code-2026-features]] (output styles·statusline·6 permission modes·slash→skills·diagnostics). Cross-linked best-practices/extension-architecture. Corrected [[claude-ecosystem]] settingSources claim (reverted per official migration guide). lint 0. **scrub's first live ingest verified.**
- **2026-06-01** — **Credential scrub** (domain-ingest safety gate). New `.claude/scrub-secrets.py` (importable core + CLI) detects/masks GitHub PAT·OpenAI/Anthropic·AWS·Google·Slack·Stripe·JWT·private-key·URL-basic-auth → `<REDACTED:name>`; conservative (clear-prefix high-confidence only + placeholder exclusion to cut false positives). New `.claude/hooks/secret-scan.py` (PostToolUse safety net) warns on secrets in just-edited text files (exit 2, reuses the core). Prevention (scrub in ingest step) is 1st line, hook is 2nd. Hook excludes `.claude/tests/` + scrub tool itself (they hold patterns as data → no nag loop). 43/43 tests pass (+11). Registered (user-approved): secret-scan in PostToolUse + scrub step in ingest-url.md. **Active now.**
- **2026-06-01** — **Security + mechanism test suite** (user picked "your recommendation"). (1) **PAT removed from `.git/config`** — moved token to osxkeychain, URL now `https://tnqls0624@github.com/...` (username only); push auth via keychain. ⚠ user must still revoke/rotate the old token on GitHub (it leaked into chat logs). (2) **`.claude/tests/test_mechanisms.py`** (stdlib unittest, 31 cases) — every hook(5)+bash-script is silent-fail(exit 0) so regressions went unseen; tests call each mechanism in an isolated temp vault and assert its contract (capture dedup, sync_push 4 git scenarios, lint-check schema/fallback, session-context boot-pick/INJECT, protect-sources allow/deny, lint/collect-questions/review-queue). Runner `run-tests.sh` wired into weekly-lint (notifies on FAILED). EXTENSIBILITY now requires a test case per new mechanism. = V-axis self-applied.
- **2026-06-01** — **Knowledge-reuse: review-queue** (design-panel workflow → C2 spaced-review + C3 backlink ranking). New `.claude/review-queue.sh` (0-LLM) ranks wiki pages into 3 buckets (trusted-but-forgotten = stable·backlink-DESC / re-review = active·needs-review / near-orphan = backlink≤1; excludes hub·MOC) → deterministic root `review-queue.md`. **Boot push**: `session-context.py` surfaces ONE rotating pick (ISO-week) from the top bucket, appended OUTSIDE the cached INJECT block (rotates only by ISO-week → cache churn bounded to ~1 miss/week, not per-session; `WIKI_NO_RECALL=1` disables). Attacks the diagnosed starved query/save (1–2%): pull-only dashboards go unread, so reuse must be pushed at boot. Piggybacks weekly-lint. Verified: deterministic body, boot pick shows, INJECT preserved, lint 0.
- **2026-06-01** — **Infra hardening** (user-chosen track after ①②). (1) auto-commit SessionEnd push now **fetch-guarded**: `git fetch` → behind==0 push / ahead==0 ff-only merge / diverged → hold push + write `sync-status.txt`, surfaced at next boot by `session-context` (multi-mac safe; 4 git scenarios tested incl. resolve→clear). (2) Frontmatter required-fields **externalized** to `.claude/wiki-required-fields.txt` — `lint.sh` + `wiki-lint-check.py` both read it (hardcoded fallback), kills the 3-place drift. Closes the HIGH sync-conflict risk flagged in this session's opening git/sync diagnosis.
- **2026-06-01** — **Passive capture + open-questions dashboard** (recommended features ①②, ultracode). `capture-candidate.py`(Stop, pre-auto-commit) queues 'knowledge-like but un-wikied' final answers to `inbox/_capture-candidates.md` (heuristic detect → `/process-inbox` LLM gate = cost-ladder; recall-biased; queue gitignored as local scratch). `collect-questions.sh` regenerates root `questions.md` from existing `## 열린 질문` + `> [!warning]` markers (no new convention). Addresses diagnosis: query/save only 1–2% (accumulates, rarely reuses). settings.json Stop registration applied (capture→auto-commit order, user-approved); E2E 5/5 + real-transcript format verified. **Active now.**
- **2026-05-31** — **Query→synthesis: system self-improvement** (workflow, 4 agents). User asked "can it self-learn?". Created [[self-improving-system]] (synthesis): **learning lives in editable artifacts (rules·skills·hooks·memory·wiki), NOT weights** — grounded in Reflexion (linguistic feedback→episodic memory, no weight update), Voyager (gradient-free skill library), Darwin Gödel Machine (self-edits own scaffolding, eval-driven, SWE-bench 20→50%), self-evolving survey. Loop = **do→evaluate→codify→compound = harness V→L→C/S** (this vault is the worked instance; log.md self-correction cases are the worked log). Honest limits (the balance): weight ceiling · self-eval validity (Huang: intrinsic self-correction fails) · bench≠success (METR 19% slower) · reward hacking · human-in-loop. Answers [[agent-harness-taxonomy]]'s V-axis open-Q. +digest. **Self-referential: the vault now has a page on its own improvement mechanism.**
- **2026-05-31** — **Semantic lint + full fix** (workflow, 9 agents: 4 dims scan→adversarial verify→synth; false-positives rejected). 0 broken/orphan/frontmatter (machine). 8 confirmed semantic issues — mostly **drift from this session's own harness surgery**: satellite pages (langgraph/smart-agent/channel-io/production-llm-backend) still used the old vendor "control/monitor/improve" frame, langgraph misclassified core axes (checkpoint=S, RetryPolicy=E) as safety slices. **All 8 fixed**: satellites realigned to 6 axes, mechanism counts → canonical-reference (drift-proof), stale counts updated (32p/17), protect-sources "보증→강제(best-effort)". Created **[[mcp]]** (canonical: transport/scope/Obsidian servers, = harness T-axis); added taxonomy back-links (one-way gap closed). agent-harness open-Qs closed.
## New entities/concepts (recent)
- [[agent-harness]] (hub, =execution scaffolding) · [[agent-harness-taxonomy]] (synthesis, H=(E,T,C,S,L,V)) · [[guardrails]] (=L axis) · [[shadow-ai]] · [[channel-io]] (entity) — agent-harness cluster (`ai-engineering`)
- [[mcp]] (tooling, =harness T-axis canonical) · [[smart-agent-case-study]] (실코드 사례 연구) · [[production-llm-backend]] · [[realtime-messaging]] · [[rag-patterns]] · [[langgraph-multi-agent]] · [[ai-engineering-moc]] (`ai-engineering` domain)
- [[self-improving-system]] (synthesis, llm-wiki — learning=artifacts not weights) · [[second-brain-architecture]] · [[context-layering]] · [[andrej-karpathy]]
